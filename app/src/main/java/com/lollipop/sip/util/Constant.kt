package com.lollipop.sip.util

class Constant {
    object Network {
        const val REQUEST_NOT_FOUND = 404
        const val REQUEST_SUCCESS = 200
    }

    object URL {
        const val IMAGE_URL = "http://sip.sha-dev.com/assets/unggah/"
        const val IMAGE_BANGUNAN = "${IMAGE_URL}bangunan/"
    }
}